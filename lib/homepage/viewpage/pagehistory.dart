import 'package:flutter/material.dart';
class pagehistory extends StatelessWidget {
  const pagehistory({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Container(child: Text("history"),));
  }
}