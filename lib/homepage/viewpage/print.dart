import 'package:flutter/material.dart';
class pageme extends StatelessWidget {
  const pageme({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Container(child: Text("me"),));
  }
}