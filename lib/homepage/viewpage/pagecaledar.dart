import 'package:flutter/material.dart';
class pagecaledar extends StatelessWidget {
  const pagecaledar({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Container(child: Text("caledar"),));
  }
}